# Frontend

Здесь будет находиться клиентское приложение на Vue 3, TypeScript и Vite.

Production-сборка `frontend/dist` будет включаться в `hr-service.war`, чтобы на
Helios не требовался отдельный Node.js-процесс.
